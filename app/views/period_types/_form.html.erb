<%= form_with(model: period_type) do |form| %>
  <% if period_type.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(period_type.errors.count, "error") %> prohibited this period_type from being saved:</h2>

      <ul>
        <% period_type.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.text_field :description %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
