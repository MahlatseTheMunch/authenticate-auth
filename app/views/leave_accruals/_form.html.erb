<%= form_with(model: leave_accrual) do |form| %>
  <% if leave_accrual.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(leave_accrual.errors.count, "error") %> prohibited this leave_accrual from being saved:</h2>

      <ul>
        <% leave_accrual.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :days_allocated, style: "display: block" %>
    <%= form.text_field :days_allocated %>
  </div>

  <div>
    <%= form.label :employee_id, style: "display: block" %>
    <%= form.text_field :employee_id %>
  </div>

  <div>
    <%= form.label :period_type_id, style: "display: block" %>
    <%= form.text_field :period_type_id %>
  </div>

  <div>
    <%= form.label :leave_type_id, style: "display: block" %>
    <%= form.text_field :leave_type_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
