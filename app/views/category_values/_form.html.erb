<%= form_with(model: category_value) do |form| %>
  <% if category_value.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(category_value.errors.count, "error") %> prohibited this category_value from being saved:</h2>

      <ul>
        <% category_value.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :category_id, style: "display: block" %>
    <%= form.text_field :category_id %>
  </div>

  <div>
    <%= form.label :value_id, style: "display: block" %>
    <%= form.text_field :value_id %>
  </div>

  <div>
    <%= form.label :skhokho_id, style: "display: block" %>
    <%= form.text_field :skhokho_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
