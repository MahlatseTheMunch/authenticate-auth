<%= form_with(model: holiday) do |form| %>
  <% if holiday.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(holiday.errors.count, "error") %> prohibited this holiday from being saved:</h2>

      <ul>
        <% holiday.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :region_id, style: "display: block" %>
    <%= form.text_field :region_id %>
  </div>

  <div>
    <%= form.label :holiday_date, style: "display: block" %>
    <%= form.date_field :holiday_date %>
  </div>

  <div>
    <%= form.label :holiday_name, style: "display: block" %>
    <%= form.text_field :holiday_name %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
