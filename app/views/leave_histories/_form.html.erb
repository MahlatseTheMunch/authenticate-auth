<%= form_with(model: leave_history) do |form| %>
  <% if leave_history.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(leave_history.errors.count, "error") %> prohibited this leave_history from being saved:</h2>

      <ul>
        <% leave_history.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.text_field :description %>
  </div>

  <div>
    <%= form.label :comment, style: "display: block" %>
    <%= form.text_area :comment %>
  </div>

  <div>
    <%= form.label :leave_id, style: "display: block" %>
    <%= form.text_field :leave_id %>
  </div>

  <div>
    <%= form.label :leave_status_id, style: "display: block" %>
    <%= form.text_field :leave_status_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
