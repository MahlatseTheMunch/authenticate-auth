<%= form_with(model: allocation_type) do |form| %>
  <% if allocation_type.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(allocation_type.errors.count, "error") %> prohibited this allocation_type from being saved:</h2>

      <ul>
        <% allocation_type.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :description, style: "display: block" %>
    <%= form.text_field :description %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
